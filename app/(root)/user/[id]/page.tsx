import { UserWrapper } from '@/components/shared/user';

export default function User() {
  return (
    <main className="flex-1 flex flex-col items-center h-[90vh]">
      <UserWrapper />
    </main>
  );
}
